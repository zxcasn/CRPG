import React from 'react'
import ReactDOM from 'react-dom/client'
import CardDeckRPG from './init'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CardDeckRPG />
  </React.StrictMode>,
)